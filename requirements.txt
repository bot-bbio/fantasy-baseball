# Core
espn-api>=0.44.1        # read-only ESPN fantasy client (baseball module)
python-dotenv>=1.0.1    # load .env config
requests>=2.32.0        # MLB Stats API calls
tabulate>=0.9.0         # pretty CLI tables
tzdata>=2024.1          # IANA timezone database (needed on Windows for zoneinfo)

# Dev / test
pytest>=8.2.0
